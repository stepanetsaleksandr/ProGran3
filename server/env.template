# ===========================================
# ProGran3 Server Environment Variables Template
# Скопіюйте цей файл в .env.local та заповніть реальними значеннями
# ===========================================

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Crypto Configuration (згенеруйте безпечний ключ)
CRYPTO_SECRET_KEY=your-very-secret-key-here-32-chars-min

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
PROGRAN3_API_URL=http://localhost:3000/api

# Admin API Keys (для захищених endpoints)
API_KEYS=your-api-key-here-64-chars
NEXT_PUBLIC_ADMIN_API_KEY=your-api-key-here-64-chars

# Database Configuration (опціонально)
DATABASE_URL=postgresql://user:password@host:port/database

# Upstash Redis - для production rate limiting (опціонально)
# Без цього використовується in-memory rate limiting
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# HMAC Configuration - для додаткового захисту запитів (опціонально)
# Має збігатися з ключем в plugin/proGran3/security/api_client.rb
HMAC_SECRET_KEY=your-hmac-secret-key-64-chars

# ===========================================
# ІНСТРУКЦІЇ:
# 1. Скопіюйте цей файл: cp env.template .env.local
# 2. Заповніть всі значення реальними даними
# 3. НІКОЛИ не комітьте .env.local в git!
# 4. Для production використовуйте інші ключі
# ===========================================
